# Linux-side requirements for the RP1 SCMI UART clock coexistence PoC.
# Apply after bcm2712_defconfig, then run olddefconfig.
# No Linux C source patch is required.
CONFIG_MAILBOX=y
CONFIG_MBOX_RP1=y
CONFIG_ARM_SCMI_PROTOCOL=y
CONFIG_ARM_SCMI_TRANSPORT_MAILBOX=y
CONFIG_COMMON_CLK_SCMI=y
